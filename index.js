/**
 * @format
 */

import {AppRegistry} from 'react-native';
import NewsApp from './src/screens/NewsList';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => NewsApp);
